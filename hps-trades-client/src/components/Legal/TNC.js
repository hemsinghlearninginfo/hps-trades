import React from 'react';
import Components from '../index';
import {iconConstants } from '../../_constants';

const TNC = () => (
    <Components.PageTemplate iconType={iconConstants.LEGAL} heading="Terms and condition">
        <div>
            Terms and Condition
        </div>
    </Components.PageTemplate>
);

export default TNC;
